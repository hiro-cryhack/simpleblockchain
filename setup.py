from setuptools import setup, find_packages

setup(
    name='simpleblockchain',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # 列出你的项目依赖的所有包
    ],
    # 添加其他元数据，如作者、项目描述等
)
